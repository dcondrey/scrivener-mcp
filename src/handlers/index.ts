/**
 * Handler types re-exported for convenience.
 *
 * Tool registration and dispatch are handled by skill-registry.ts.
 */

export { HandlerContext, HandlerError, HandlerResult } from './types.js';
